<span id="average_speed">
Avg. speed: <%= current_user.runs.average(:avgspeed).round(1) %> km/h (<%= current_user.runs.average (:avgspeed_comparison).round(1) %>%)
</span> 

<span id="average_distance">
Avg. distance: <%= current_user.runs.average(:distance).round(1) %> km (<%= current_user.runs.average(:distance_comparison).round(1) %>%)
</span>

<span id="overall_performance">
<%= current_user.runs.average(:total_comparison).round(1) %>%
</span>